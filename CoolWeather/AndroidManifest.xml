<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coolweather.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.ChooseAreaActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.WeatherActivity" >
        </activity>
        <activity android:name=".activity.MainActivityGroup" >
        </activity>
        <activity android:name=".activity.MyActivity" >
        </activity>
        <activity android:name=".activity.SceneActivity" >
        </activity>
        <activity android:name=".activity.WeatherFragmentActivity" >
        </activity>
        <activity android:name=".activity.UserList" >
        </activity>

        <service android:name=".service.AutoUpdateService" >
        </service>

        <receiver android:name=".receiver.AutoUpdateReceiver" >
        </receiver>

        <activity
            android:name="com.coolweather.app.activity.WeatherViewPager"
            android:label="@string/title_activity_weather_view_pager" >
        </activity>
    </application>

</manifest>